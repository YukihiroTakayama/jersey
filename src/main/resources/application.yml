spring:
  profiles:
    active: localhost

---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jersey_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

---
spring:
  datasource:
    url: jdbc:mysql://db/jersey_db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

